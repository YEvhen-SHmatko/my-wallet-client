export const BaseURL = 'https://smart-finance.goit.co.ua/api/v1';

export const AuthByGoogle = `${BaseURL}/auth/google`;

export const Login = `${BaseURL}/auth/login`;

export const Register = `${BaseURL}/auth/register`;

export const Logout = `${BaseURL}/auth/logout`;

export const Income = `${BaseURL}/income`;

export const Balance = `${BaseURL}/balance`;

export const Categories = `${BaseURL}/categories`;

export const Costs = `${BaseURL}/costs`;

export const Products = `${BaseURL}/products`;
